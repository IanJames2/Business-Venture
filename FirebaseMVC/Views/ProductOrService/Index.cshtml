@model IEnumerable<BusinessVenture.Models.ViewModels.ProductOrServiceFormViewModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>

            <th>
                @Html.DisplayNameFor(model => model.Business.Title)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ProductOrService.NameOfProductOrService)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ProductOrService.Cost)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>

            <td>
                @Html.DisplayFor(modelItem => item.Business.Title)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ProductOrService.NameOfProductOrService)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ProductOrService.Cost)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.ProductOrService.Id }) |
                @Html.ActionLink("Details", "Details", new { item.ProductOrService.Id }) |
                @Html.ActionLink("Delete", "Delete", new { item.ProductOrService.Id })
            </td>
        </tr>
}
    </tbody>
</table>
